# Query Doctor — سجل تطوير المشروع
## micro1 Agentic Workflows Hackathon

---

## 🎯 فكرة المشروع
وكيل ذكاء اصطناعي يقرأ كود مشروع Laravel، يكتشف مشاكل أداء Eloquent (N+1 queries، فهارس ناقصة)، يقترح إصلاحات فعلية، ثم يتحقق من التحسن رقمياً عبر تشغيل الاختبارات قبل وبعد.

**المستخدم المستهدف:** مطور/فريق Laravel يرث كوداً به مشاكل أداء غير مكتشفة.

**عنق الزجاجة:** اكتشاف N+1 يدوياً بطيء ويعتمد على خبرة المطور؛ لا توجد أداة تربط الكشف بالإصلاح والتحقق في حلقة واحدة تلقائية.

---

## ✅ ما تم إنجازه حتى الآن

### 1. إعداد البنية التحتية
- إنشاء مشروع Laravel جديد
- ربط Laravel AI SDK بالمشروع
- إعداد الاتصال بـ **Gemini API** و**DeepSeek API** — تم التحقق من نجاح الاتصال مع كلا المزوّدين

### 2. قرار تقني: اختيار الوكيل الرئيسي
- **Gemini** = المحرك الرئيسي للتحليل واتخاذ القرار (سياق أوسع + استدعاء أدوات أكثر استقراراً)
- **DeepSeek** = خيار احتياطي/اقتصادي لتشغيل تكرارات إضافية دون استهلاك الميزانية

---

## 🔜 العمل القادم (بالترتيب)

### المرحلة 1: قاعدة البيانات (Models / Migrations / Seeders)
- [ ] تصميم مخطط "متجر إلكتروني": `categories → products → orders → order_items → reviews`
- [ ] إنشاء الـ Migrations (بدون فهرس على `category_id` عمداً — لإظهار مشكلة أداء لاحقاً)
- [ ] إنشاء Models مع العلاقات (`hasMany`, `belongsTo`) كاملة وواضحة
- [ ] إنشاء Factories واقعية لكل Model
- [ ] بناء Seeder بحجم كافٍ (500 منتج، 2000 طلب، 5000+ عنصر طلب) مع **seed ثابت** لضمان إعادة الإنتاج

### المرحلة 2: بناء الـ Endpoints (حالات الاختبار)
- [ ] بناء 10 endpoints/استعلامات تمثل استخدامات حقيقية (مثل `GET /api/orders`)
- [ ] تفعيل `DB::listen()` لتسجيل كل استعلام تلقائياً

### المرحلة 3: قياس الـ Baseline
- [ ] تشغيل الحالات العشر وتسجيل: عدد الاستعلامات، الزمن (ms) لكل حالة
- [ ] حفظ هذه الأرقام كمرجع (baseline) للمقارنة لاحقاً

### المرحلة 4: بناء الوكيل (Tool/Function Calling)
- [ ] أداة `read_file` — قراءة محتوى ملف من المشروع
- [ ] أداة `list_files` — عرض ملفات مجلد
- [ ] أداة `run_command` — تشغيل أوامر محددة فقط (whitelist: `php artisan test`, إلخ)
- [ ] ربط الأدوات بـ Gemini API عبر function declarations
- [ ] كتابة system prompt يوجّه الوكيل لفحص Controllers ثم تشغيل الاختبارات

### المرحلة 5: التسجيل (Logging)
- [ ] كل استدعاء أداة يُسجَّل تلقائياً في ملف JSON منفصل لكل تشغيل (`logs/run_<timestamp>.json`)
- [ ] تسجيل: القرار، الأداة المستدعاة، المدخلات، الناتج، الوقت

### المرحلة 6: حلقة التحقق والتكرار
- [ ] تطبيق الإصلاحات المقترحة بعد موافقة بشرية
- [ ] إعادة تشغيل نفس الحالات العشر وقياس التحسن
- [ ] بناء جدول Changelog (المرحلة | ما جُرّب | النتيجة | القرار) — بما في ذلك أي محاولة فشلت

### المرحلة 7: التسليم النهائي
- [ ] README (المستخدم، المشكلة، الحل، Changelog، أهم فشل ودرس مستفاد)
- [ ] Reproduction Guide (أوامر دقيقة من بيئة نظيفة)
- [ ] فيديو ≤5 دقائق
- [ ] Agent Trajectories (تحويل ملفات JSON إلى تقرير مقروء)

---

## ⚠️ أخطاء يجب تفاديها (دروس مسبقة)
- عدم وضع حد أقصى لعدد خطوات الوكيل → خطر حلقة لانهائية
- السماح للوكيل بتنفيذ أي أمر حرفياً → خطر أوامر مدمرة (استخدم whitelist)
- تصديق تقرير الوكيل النصي دون تحقق برمجي فعلي من الأرقام
- إرسال المشروع كاملاً في طلب واحد → تجاوز حجم السياق
- عدم تثبيت seed البيانات → نتائج غير قابلة لإعادة الإنتاج
- تأجيل التوثيق للنهاية → نسيان تفاصيل القرارات

---

*آخر تحديث: 29 أغسطس 2026*